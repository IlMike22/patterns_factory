class WebButton: IButton {
    override fun render() {
       // render web button
        println("web button will now be rendered...")
    }

    override fun onClick() {
        // define click event for web button
        println("web button is clicked...")
    }
}