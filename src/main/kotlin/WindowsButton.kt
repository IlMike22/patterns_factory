class WindowsButton: IButton {
    override fun render() {
        // render windows button
        println("web button will now be rendered...")
    }

    override fun onClick() {
        // define click event for windows button
        println("web button is clicked...")
    }
}